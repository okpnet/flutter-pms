# 0028 実施ログ(要件0024の実行・views.mdの評価レポートのみ・生成なし)

指示:
> 0024の実行、0028を評価して生成はせず、モレや重複、提案の状態のレポートを出力する。

要件0028(CLAUDE.md)のとおり、本ログでは **GraphQLファイルの作成・build_runnerの実行は一切行っていない**。
[views.md](../source/views.md)(2026/09/16時点、未コミットの編集途中版)と[schema.graphql](../lib/graphql/schema.graphql)を
突き合わせた評価結果のみを記録する。

## 1. 要件0024の実行結果(schema.graphqlの変更確認)

| 項目 | 状態 |
|---|---|
| `lib/graphql/schema.graphql` | 389,298行(要件0026・追記2で取得した最新SDLのまま、未変更)。 |
| `lib/postgraphile/schema.graphql.dart`(本環境のローカル生成物) | 更新日時が古く(直近のbuild_runner実行記録が無い環境)、要件0026・追記1/2で反映されたschema.graphql側の変更を反映できていない可能性が高い。 |

→ サーバへの再取得(要件0026)は今回の指示に含まれないため実施していない。`schema.graphql`自体はサーバ由来の内容から
変わっていないため「サーバ側スキーマの変更」は無いが、**本環境のモデル生成物が最新のschema.graphqlに追随できていない
可能性がある**ことを記録する(要件0015のとおりgit管理対象外のため環境ごとに再生成が前提)。指示により生成は行っていない。

## 2. モレ(要件0027・0028の新ルールに基づくエラー相当)

### 2-1. readに削除フラグ列が無い(要件0028:「readに削除フラグ列の指定がないときは、エラーとして記録する」)

| 見出し2 | 状態 |
|---|---|
| `operation` | `### read`に「削除」列が無い。他の全画面のreadには本日の編集で追加済みだが、この画面のみ未追加。 |
| `equipment_inspection_operation` | 同上。「削除」列が無い。 |

いずれも同一物理テーブルを共有する編集用画面(`oepration_editer`/`equipment_inspection_operation_editor`)の
readには「削除」が追加済みであり、読み取り専用側のみ取り残されている。**エラーとして記録する。**

### 2-2. readが配列(1対多のリレーションシップ)をそのまま取得している(要件0027:「readで配列を読み込む要求があるときはエラーとして記録する」)

| 見出し2 | 該当項目 | 内容 |
|---|---|---|
| `info_company` | `### read`「事業所のリスト」 | 説明に「配列のまま。フラットにしない」と明記。 |
| `oepration_editer` | `### read`「工程運用課題」 | `mstrOperationTasksByMstrOperationId`(Connection型、schema.graphql 172156行)を参照。1対多。 |
| `equipment_inspection_operation_editor` | `### read`「点検項目」 | `mstrInspectionOperationTasksByMstrInspectionOperationId`(Connection型、schema.graphql 154045行)を参照。1対多。 |

**エラーとして記録する。** 参考: `task`の「工程場所」は本日の編集で「(先頭)」+「工程場所数」に変更され、配列を
そのまま返す形を回避済み(同種の対応の前例)。上記3件も同様に、①先頭1件+件数へ変換する、②RFE専用に移す、
③配列のままreadで返す設計を維持する、のいずれかをユーザーに判断いただく必要がある。

### 2-3. RFE(read for editing)セクションが未着手

`edit`を持つ全17見出し(info_company, info_office, info_department, info_staff, info_position,
org_res_equipment, org_res_equipment_kind, org_res_location, customer, customer_contact, supplier,
supplier_contact, shipping_kind, item_kind, manufacture, item_actual_size, item, task_group, task,
oepration_editer, equipment_inspection_kind, equipment_inspection, equipment_inspection_operation_editor,
equipment_inspection_peration_task)のいずれにも、要件0027・0028が定める"read for editing"(RFE)の
見出し3(`###`)がまだ存在しない。

このため要件0028後段の「RFEのidを引数にとる条件が一意として成立しないときは…」の評価は、RFE自体が
未定義のため**現時点では実施できない**。RFE定義が追加された時点で再評価が必要。

参考: `oepration_editer`・`equipment_inspection_operation_editor`の`### read`は、編集対象を1件、配列項目
込みで取得している点で、内容的にはRFEに近い(2-2節の配列項目はRFEの守備範囲と整合する)。これらを正式な
RFE(`### read for editing`、命名規則どおりファイル名`##_###_rfe`)として再定義できないか、という提案として
記録する(採否はユーザー判断、本文への反映は行っていない)。

## 3. 重複・誤記(ドキュメント品質、生成物の誤りに直結しうるもの)

| 見出し2 | 箇所 | 内容 |
|---|---|---|
| `info_position` | `### read`「削除」の説明 | 「担当者情報を対象」となっているが誤り(`info_staff`からのコピペ跡)。正しくは「ポジション情報を対象」。本日の削除フラグ追加で新たに混入。 |
| `org_res_location` | `### edit`「備考」「更新者id」「更新者履歴id」「削除」、および本日追加の`### read`「削除」 | いずれも説明が「設備分類マスタを対象」になっている(`org_res_equipment_kind`からのコピペ跡、計5箇所)。正しくは「場所マスタを対象」。**要件0025評価では`org_res_location`は「既存8画面」として対象外(要件0020)だったため、これまで一度も指摘されていない。** |
| `equipment_inspection_peration_task` | `### edit`「備考」「更新者id」「更新者履歴id」「削除」 | いずれも説明が「点検整備運用マスタを対象」になっている(計4箇所)。物理テーブルは点検整備運用**課題**マスタ(`MstrInspectionOperationTask`)であり、同画面の`### read`「更新日」「更新者名」の説明(「点検整備運用課題」)と食い違っている。`equipment_inspection_operation_editor`からのコピペ跡とみられる。 |
| `equipment_inspection_peration_task` | `### read`「点検整備運用略称」(2つ目、「点検整備コード/名/読み」の直後) | ラベルが直前の「点検整備運用略称」(点検整備運用マスタの略称)と重複しているが、説明文は「点検整備マスタの呼称セットの略称」であり指しているテーブルが異なる。パターン(`点検整備名`→`点検整備読み`→`点検整備略称`)から見て、正しいラベルは「点検整備略称」であるべき。 |
| `org_res_equipment` | `### read`「表示コード」の行 | `1.  表示コード1.  呼称セットid`のように2つの箇条書きが改行なしで連結されてしまっている(フォーマット不良)。`### edit`側は正しく分離されている。 |

## 4. 前回(要件0025)評価時点のエラーが解消済みと見られるもの(参考・良い知らせ)

要件0025評価ログ・0025_task_operation評価ログで「変換不可能なエラー」として生成見送りとした項目のうち、
本日時点のviews.mdでは以下がいずれも解消されているように見える(views.md側の修正が先行している)。

| 見出し2 | 旧エラー | 現状 |
|---|---|---|
| `customer` / `supplier` | editに存在しない「親場所マスタ」参照(4-1) | 該当記述が削除済み。 |
| `item_actual_size` | editに存在しない「呼称セット」参照(4-2) | 該当記述が削除済み。「大きさ区分id」相当は「品目大きさ区分(オブジェクト)」として存在。 |
| `equipment_inspection` | 存在しない「工程」参照、必須列`inspectionKind`のモレ(4-1) | 「工程」関連3行は取り消し線で除外済み。「点検整備区分id」がread/editとも追加済み。 |
| `equipment_inspection_peration_task` | ファイル末尾欠落(更新者履歴id/削除が無い)、editの必須`mstrInspectionId`欠落、2階層先参照の設計不明(4-2) | 「更新者履歴id」「削除」が追加済み。「点検整備(オブジェクト)」が追加済み。「外部点検整備」「点検整備サービス品目id」は「(表示のための参照)」と明記され設計意図が明確化された。 |

**次回、要件0025相当の再評価(モレ・エラー無し確認)を行えば、これら4見出しをGraphQL生成対象に追加できる
可能性が高い。**(本ログでは生成は行っていない。)

## 5. 提案の状態

### 5-1. views.md本文に記録済みの`### 提案への指示`(3件、いずれも採択)

| 見出し2 | 提案内容 | 状態 |
|---|---|---|
| `customer` | read・editへ`isCustomer: true`を固定条件として含める | 採択 |
| `supplier` | read・editへ`isSupplier: true`を固定条件として含める | 採択 |
| `customer_contact` | editへ「Eメールアドレス」を追加(supplier_contactとの不整合解消) | 採択 |

### 5-2. ログのみに記録され、views.md本文へ`### 提案への指示`として未反映のもの

要件0025の手順は「提案を適用するときは"##"へ"###提案への指示"として指示を追加する」だが、以下は
[0025_task_operation_inspection_evaluation_log.md](0025_task_operation_inspection_evaluation_log.md)の
5節に記録されたのみで、views.md側に対応する`### 提案への指示`が無い。

| 見出し2 | 内容 | 現状 |
|---|---|---|
| `task` | 「工程場所」の1対多を配列のまま取得する設計判断(5-1) | 本日「(先頭)」+「工程場所数」へ変更されており、当初の提案(配列のまま)とは異なる対応が取られている。提案自体が上書きされた状態で、views.mdへの正式な記録が無い。 |
| `task` | 「親id」編集への対応(5-2、2026/09/15対応済み) | views.md本文の変更(「idのみセットする」の追記)はあるが`### 提案への指示`は無い。 |
| `operation` | 「工数」を配列取得+アプリ側getterで算出する設計(5-3) | views.md本文に「getプロパティで実装」の追記はあるが`### 提案への指示`は無い。 |

**提案として記録する:** 上記3件について、`### 提案への指示`をviews.mdへ追加するかどうかの判断を推奨する
(手順の一貫性のため)。エラーとしては扱わない。

### 5-3. 新規の提案(列命名規則との整合)

CLAUDE.mdのGraphQL変換ルールに「列(結合)はカンマ区切りの文字列とする」という規則が追加されたが、
`customer`/`supplier`の「提供」、`item`の「品目提供」は説明文で「カンマ区切りで結合」と述べているのみで、
ラベル自体に「(結合)」が付いていない。表記統一の提案として記録する(エラーとしては扱わない)。

## 6. 結論

- 要件0024: schema.graphql自体の変更は無し。ローカル生成物(schema.graphql.dart)が最新化されているかは
  本環境では未確認・生成未実施。
- モレ(エラー相当、2節): 削除フラグ未設定2件、read内配列3件、RFE未着手17件。
- 重複・誤記(3節): 5件(うち1件はコピペの取りこぼしが過去の評価を一度もすり抜けていた`org_res_location`)。
- 良い知らせ(4節): 前回見送りだった4見出しのエラーは解消済みと見られる。
- 提案(5節): 採択済み3件、views.md未反映3件、新規提案1件。

本ログの指示どおり、GraphQL作成・build_runner実行は行っていない。次のアクションはユーザーの判断を仰ぐ。

## 追記1: 0028再評価(2026/09/16、「0028の再評価」の再指示)

views.mdが更新されたため、本文の現状(未コミット)と上記2〜5節を突き合わせて再評価した。
本追記でも**GraphQL作成・build_runnerの実行は行っていない**。

### 解消済み(views.md側で対応済みと確認できたもの)

| 節 | 内容 | 状態 |
|---|---|---|
| 2-1 | `operation`のread「削除」欠落 | 追加済み |
| 2-1 | `equipment_inspection_operation`のread「削除」欠落 | 追加済み(ただし下記の新規指摘参照) |
| 2-2 | `oepration_editer`のread「工程運用課題」が配列 | 「工程運用課題id(先頭)」+「工程運用課題数」に変更され解消 |
| 3-1 | `info_position`read「削除」の説明誤り(「担当者情報を対象」) | 「ポジション情報を対象」に修正済み |
| 3-2 | `org_res_location`の「設備分類マスタ」コピペ誤り(edit4箇所) | 「場所マスタ」に修正済み(ただし下記の新規指摘参照) |
| 3-3 | `equipment_inspection_peration_task`の「点検整備運用マスタ」誤り(edit4箇所) | 「点検整備運用課題マスタ」に統一され解消。readの「削除」も同様に正しい表記で追加済み |
| 3-5 | `org_res_equipment`read「表示コード」の行結合 | 2行に分割され解消 |
| 5-2 | `task`/`operation`の提案がviews.md未反映 | `## task`・`## operation`それぞれに`### 提案への指示`が追加され反映済み |
| 5-3 | 「提供」のラベルに「(結合)」が無い | `customer`・`supplier`の「提供」は「提供(結合)」に修正済み(ただし`item`「品目提供」は below 参照) |

### 未解消・新規に見つかったもの

1.  **`item`「品目提供」に「(結合)」が未反映**(5-3の一部のみ対応)。`customer`/`supplier`の「提供」は
    修正されたが、`item`の「品目提供」(read、752行台)はラベルがそのまま。表記統一のため要確認。
2.  **`equipment_inspection_operation_editor`read「点検項目」が依然として配列**
    (`点検整備運用課題のオブジェクト`、`oepration_editer`と同種の設計だが未修正)。2-2節のエラーのうち
    この1件のみ未対応。
3.  **`info_company`read「事業所のリスト」が依然として配列**(「配列のまま。フラットにしない」の記述も
    そのまま)。2-2節のエラーのうち未対応。
4.  **新規誤記: `org_res_location`read「削除」の説明が「場所マスタマスタを対象」**(「マスタ」が
    二重になっている)。今回の修正作業中に混入したとみられる単純な誤記。
5.  **新規に判明: `equipment_inspection_operation`(readのみの画面)の「更新日」「更新者名」、および
    今回追加された「削除」の3項目すべてが「点検整備マスタを対象/の更新日/が参照する…」になっている。**
    物理テーブルは点検整備**運用**マスタ(`MstrInspectionOperation`)であり、兄弟画面
    `equipment_inspection_operation_editor`の同じ3項目は正しく「点検整備運用マスタ」になっている。
    「運用」が抜け落ちたコピペ誤りとみられる(「更新日」「更新者名」は今回追加分ではなく既存の誤記で、
    前回の評価では見落としていた)。
6.  **`equipment_inspection_peration_task`read「点検整備運用略称」の重複ラベル(3-4節)は未修正のまま**
    (1153行台と1161行台に同じラベルが2回登場し、2回目は「点検整備マスタの呼称セットの略称」を指している)。
7.  **`task`・`operation`の`### 提案への指示`に、customer/supplier/customer_contactのような明示的な
    「採択」の記載が無い。** 内容は本文へ反映済みで実質的には採択されているが、記録の体裁が他画面と
    異なる(手順上の一貫性の観点で軽微)。
8.  RFE(read for editing)セクションは引き続き全17見出しで未着手(2-3節から変化なし)。

### 再評価まとめ

| 分類 | 前回(本文2〜5節) | 今回 |
|---|---|---|
| read削除フラグ欠落 | 2件 | 0件(解消) |
| readの配列(エラー) | 3件 | 2件(`info_company`, `equipment_inspection_operation_editor`が未解消) |
| 重複・誤記 | 5件 | 3件解消・2件新規判明(4, 5)・1件未修正(6) → 実質4件が残存 |
| RFE未着手 | 17件 | 17件(変化なし) |

指示どおり生成は行っていない。次のアクションはユーザーの判断を仰ぐ。

## 追記2: 0028再評価(2026/09/16、GraphQL変換ルール更新後の「0028を生成せずに実行してモレや重複など再評価」)

views.mdがさらに更新されたため再評価した。本追記でも**GraphQL作成・build_runnerの実行は行っていない**。
CLAUDE.mdの`GraphQL変換ルール`本文自体は前回確認した内容(要件0027・0028で追加済みの
RFE規則・列(先頭)/列(結合)・削除フラグ引数)から変化はなかった。

### 解消済み(追記1で指摘した項目)

| 追記1での指摘 | 対応内容 |
|---|---|
| `item`「品目提供」に「(結合)」未反映 | 「品目提供(結合)」に修正済み |
| `equipment_inspection_operation_editor`read「点検項目」が配列 | `### read`セクション自体を削除し、`### 提案への指示`
  「編集のみで、RFEを定義したためreadを削除。」を追加。`edit`の必須引数(先頭id)からRFEが暗黙に導出される設計
  ([GraphQL変換ルール](../CLAUDE.md#GraphQL変換ルール)の「指示が無い限り、取得する情報はeditと同じでidが一致する
  1レコードのみを取得する(列の配列を除く)」)に沿った対応で、配列列(点検項目)はRFEの規則により自動的に除外される
  ため矛盾はない。 |
| `org_res_location`read「削除」の「場所マスタマスタ」誤字 | 「場所マスタ」に修正済み |
| `equipment_inspection_operation`の「点検整備マスタ」誤り(更新日・更新者名・削除) | 「点検整備運用マスタ」に修正済み |
| `equipment_inspection_peration_task`read「点検整備運用略称」の重複ラベル | 2つ目を「点検整備略称」に修正済み |

### 設計理解の訂正(重要)

`equipment_inspection_operation_editor`の対応を見て、RFEに関する理解を訂正する。当初「editを持つ全17見出しで
RFEが未着手」を一律にモレとして扱ったが、[GraphQL変換ルール](../CLAUDE.md#GraphQL変換ルール)の
「read for editing=>RFE」規則は**「指示が無い限りeditと同じ内容(配列列を除く)をidキーで1件取得する」を
デフォルト動作**として定義しているため、通常のスクリーンではviews.mdにRFE用の`###`見出しを個別に書く必要はなく、
デフォルト規則どおりGraphQL生成時に自動導出されればよい。`### 提案への指示`(または将来のRFE専用見出し)が
必要になるのは、今回の`equipment_inspection_operation_editor`のように**デフォルトから逸脱する場合
(配列列の扱い、旧readの廃止など)のみ**である。

→ 「RFE未着手17件」は実質的にモレではない(デフォルト規則が適用されるため)。ただし、次にGraphQL/モデル
生成を行う段階で、各画面のeditに「リレーションシップがある列」(GraphQL変換ルール新設の「個別指示する」対象)が
無いか確認し、ある場合はRFEでの扱い(配列除外可否、個別指定の要否)を都度判断する必要がある。

### 未解消(引き続き残る項目)

1.  **`info_company`read「事業所のリスト」は依然として配列。** ただし今回、`### 提案への指示`
    「このクエリはテーブル表示に使用せず専用ページに配置するため、配列のまま保持しフラットにはしない」が
    追加され、意図的な設計判断として記録された。要件0027の「readで配列を読み込む要求はエラー」という原則への
    明示的な例外として扱ってよいと考えられる(customer/supplier/customer_contactの提案と同種の位置づけ)。
    **エラーとしては解除し、提案として扱う。**
2.  **新設の`### 提案への指示`(4件: `info_company`, `task`, `operation`,
    `equipment_inspection_operation_editor`)のいずれにも、既存3件(customer/supplier/customer_contact)に
    ある明示的な「採択」の記載が無い。** 内容は本文へ反映済みで実質的に採択されているとみなせるが、記録の
    体裁が統一されていない(軽微、手順の一貫性の観点のみ)。
3.  RFE専用見出しは無いが、上記「設計理解の訂正」のとおりデフォルト規則で対応可能なため、次のGraphQL生成
    段階で画面ごとに要確認とする(モレとしては解消)。

### 再評価まとめ(累計)

| 分類 | 初回(本文2〜5節) | 追記1 | 追記2(今回) |
|---|---|---|---|
| read削除フラグ欠落 | 2件 | 0件 | 0件 |
| readの配列(エラー) | 3件 | 2件 | 0件(`info_company`は提案化、他2件は解消) |
| 重複・誤記 | 5件 | 4件相当 | 0件 |
| RFE未着手(モレ扱い) | 17件 | 17件 | 0件(デフォルト規則適用と理解を訂正) |
| 提案の体裁不統一(採択未記載) | – | 2件(task, operation) | 4件(info_company, task, operation, equipment_inspection_operation_editor) |

指示どおり生成は行っていない。残る論点は「提案の体裁不統一」(軽微)のみであり、views.mdは次のGraphQL
作成・build_runner実行の段階に進める状態に近いと判断する。次のアクションはユーザーの判断を仰ぐ。
