# oepration_editer画面 - Edit(ミューテーション)
#
# 画面仕様: source/views.md #oepration_editer / edit
# 作成ルール: CLAUDE.md 要件0006・0011・0017・0018・0021・0025
# 配置場所: lib/graphql/oepration_editer/oepration_editer_edit.graphql
#
# ⚠ 実スキーマの型名は"MstrOperation"。
# ⚠ 「工程運用課題」は1対多×1対多のため、追加/削除を含む配列の入れ子ミューテーションは
#   本バージョンでは未対応。読み取り専用として出力にのみ含める。
# ⚠ 更新者id/更新者履歴id/削除は0019の既定方針により変数から除外している。

mutation OeprationEditerEdit(
  $mstrOperationId: UUID!
  $controlCode: String
  $detail: String
  $remarks: String
  $symbol: String
  $sharedAppellationsId: UUID!
  $nameSharedDictionaryId: UUID!
  $nameJa: String
  $nameEn: String
  $pronunciationSharedDictionaryId: UUID!
  $pronunciationJa: String
  $pronunciationEn: String
  $nicknameSharedDictionaryId: UUID!
  $nicknameJa: String
  $nicknameEn: String
) {
  updateMstrOperationByMstrOperationId(
    input: {
      mstrOperationId: $mstrOperationId
      mstrOperationPatch: {
        controlCode: $controlCode
        detail: $detail
        remarks: $remarks
        symbol: $symbol

        # --- 呼称セット(共通名前仕様、shared_appellations経由でshared_dictionaryへ入れ子更新) ---
        sharedAppellationToSharedAppellationsId: {
          updateBySharedAppellationsId: {
            sharedAppellationsId: $sharedAppellationsId
            sharedAppellationPatch: {
              sharedDictionaryToSharedDictionaryNameId: {
                updateBySharedDictionaryId: {
                  sharedDictionaryId: $nameSharedDictionaryId
                  sharedDictionaryPatch: { ja: $nameJa, en: $nameEn }
                }
              }
              sharedDictionaryToSharedDictionaryPronunciationId: {
                updateBySharedDictionaryId: {
                  sharedDictionaryId: $pronunciationSharedDictionaryId
                  sharedDictionaryPatch: { ja: $pronunciationJa, en: $pronunciationEn }
                }
              }
              sharedDictionaryToSharedDictionaryNicknameId: {
                updateBySharedDictionaryId: {
                  sharedDictionaryId: $nicknameSharedDictionaryId
                  sharedDictionaryPatch: { ja: $nicknameJa, en: $nicknameEn }
                }
              }
            }
          }
        }
      }
    }
  ) {
    mstrOperation {
      mstrOperationId
      controlCode
      detail
      remarks
      symbol

      # --- 工程運用課題(読み取り専用。編集は別ミューテーションで対応、⚠参照) ---------
      mstrOperationTasksByMstrOperationId {
        nodes {
          mstrOperationTaskId
          mstrTaskId
          sequence
          mstrItemOperationTasksByMstrOperationTaskId {
            nodes {
              mstrItemOperationTaskId
              mstrItemId
              sequence
            }
          }
        }
      }

      sharedAppellationBySharedAppellationsId {
        sharedAppellationsId
        sharedDictionaryBySharedDictionaryNameId {
          sharedDictionaryId
          ja
          en
        }
        sharedDictionaryBySharedDictionaryPronunciationId {
          sharedDictionaryId
          ja
          en
        }
        sharedDictionaryBySharedDictionaryNicknameId {
          sharedDictionaryId
          ja
          en
        }
      }
    }
  }
}
