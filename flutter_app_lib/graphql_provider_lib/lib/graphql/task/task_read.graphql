# task画面 - Read(一覧・TrinaGrid用ページネーションクエリ)
#
# 画面仕様: source/views.md #task / read
# 作成ルール: CLAUDE.md 要件0006・0011・0017・0018・0021・0025
# 配置場所: lib/graphql/task/task_read.graphql
#
# ⚠ 実スキーマの型名は"MstrTask"(工程マスタ)。
# ⚠ 「親id」「子の数」は"MstrTaskTree"(工程体系マスタ)経由。1タスクにつき「自分が対象」の
#   ツリー行は1件のみ存在する前提とし(org_res_location等と同じ、場所/組織体系マスタと同様の設計)、
#   `mstrTaskTreesByMstrTaskId(first: 1)`の先頭ノードから親IDを取得する。子の数は
#   `mstrTaskTreesByParentMstrTaskId`のtotalCountで取得する(要件0025評価: 先行実装の
#   org_res_location/info_departmentではこの「子の数」相当が未実装のままだったため、今回は
#   totalCountで正しく実装した)。
# ⚠ 「工程場所」(場所/使用設備/供給者)は"MstrTaskLocation"経由で1対多(1つの工程が複数の
#   場所・設備・供給者候補を持ちうる)。views.mdは単一値であるかのように記述しているが、
#   実スキーマ上は配列のため、nodesをそのまま取得する(要件0025評価ログ参照。品目提供/提供と
#   同様、単一値に丸めず配列のまま扱う)。

query TaskRead(
  $first: Int!
  $offset: Int
  $condition: MstrTaskCondition
  $orderBy: [MstrTasksOrderBy!] = [PRIMARY_KEY_ASC]
  $ja: Boolean!
  $en: Boolean!
) {
  allMstrTasks(
    first: $first
    offset: $offset
    condition: $condition
    orderBy: $orderBy
  ) {
    totalCount
    pageInfo {
      hasNextPage
      endCursor
    }
    nodes {
      mstrTaskId
      code
      mstrTaskGroupId

      # --- 詳細 / クラス / 標準時間 ------------------------------------------------
      detail
      class
      defaultTime {
        seconds
        minutes
        hours
        days
        months
        years
      }

      # --- 共通項: remarks / update_at / remove ---------------------------------
      remarks
      updateAt
      remove

      # --- 親id / 子の数(工程体系マスタ、⚠参照) -----------------------------------
      mstrTaskTreesByMstrTaskId(first: 1) {
        nodes {
          mstrTaskTreeId
          parentMstrTaskId
        }
      }
      mstrTaskTreesByParentMstrTaskId {
        totalCount
      }

      # --- 工程名 / 読み / 略称(共通名前仕様) -------------------------------------
      sharedAppellationBySharedAppellationsId {
        sharedAppellationsId
        sharedDictionaryBySharedDictionaryNameId {
          sharedDictionaryId
          ja @include(if: $ja)
          en @include(if: $en)
        }
        sharedDictionaryBySharedDictionaryPronunciationId {
          sharedDictionaryId
          ja @include(if: $ja)
          en @include(if: $en)
        }
        sharedDictionaryBySharedDictionaryNicknameId {
          sharedDictionaryId
          ja @include(if: $ja)
          en @include(if: $en)
        }
      }

      # --- 工程グループコード / 名 / 読み / 略称(参照のみ) --------------------------
      mstrTaskGroupByMstrTaskGroupId {
        mstrTaskGroupId
        code
        sharedAppellationBySharedAppellationsId {
          sharedDictionaryBySharedDictionaryNameId {
            ja @include(if: $ja)
            en @include(if: $en)
          }
          sharedDictionaryBySharedDictionaryPronunciationId {
            ja @include(if: $ja)
            en @include(if: $en)
          }
          sharedDictionaryBySharedDictionaryNicknameId {
            ja @include(if: $ja)
            en @include(if: $en)
          }
        }
      }

      # --- 工程場所(⚠1対多、配列のまま) -------------------------------------------
      mstrTaskLocationsByMstrTaskId {
        nodes {
          mstrTaskLocationId
          mstrLocationByMstrLocationId {
            mstrLocationId
            code
            sharedAppellationBySharedAppellationsId {
              sharedDictionaryBySharedDictionaryNameId {
                ja @include(if: $ja)
                en @include(if: $en)
              }
              sharedDictionaryBySharedDictionaryPronunciationId {
                ja @include(if: $ja)
                en @include(if: $en)
              }
              sharedDictionaryBySharedDictionaryNicknameId {
                ja @include(if: $ja)
                en @include(if: $en)
              }
            }
          }
          mstrEquipmentByMstrEquipmentId {
            mstrEquipmentId
            code
            mstrItemByMstrItemId {
              sharedAppellationBySharedAppellationsId {
                sharedDictionaryBySharedDictionaryNameId {
                  ja @include(if: $ja)
                  en @include(if: $en)
                }
                sharedDictionaryBySharedDictionaryPronunciationId {
                  ja @include(if: $ja)
                  en @include(if: $en)
                }
                sharedDictionaryBySharedDictionaryNicknameId {
                  ja @include(if: $ja)
                  en @include(if: $en)
                }
              }
            }
          }
          mstrStakeholderByMstrStakeholderId {
            mstrStakeholderId
            code
            sharedAppellationBySharedAppellationsId {
              sharedDictionaryBySharedDictionaryNameId {
                ja @include(if: $ja)
                en @include(if: $en)
              }
              sharedDictionaryBySharedDictionaryPronunciationId {
                ja @include(if: $ja)
                en @include(if: $en)
              }
              sharedDictionaryBySharedDictionaryNicknameId {
                ja @include(if: $ja)
                en @include(if: $en)
              }
            }
          }
        }
      }

      # --- 共通項: 更新者名(共通名前仕様)の呼称セットID -----------------------------
      # ⚠ 既知の制約により「TaskReadUpdaterAppellation」を別クエリで呼び出す。
      historyInfoStaffByUpdateUserHistoryIdAndUpdateUserId {
        historyId
        infoStaffId
        sharedAppellationsId
      }
    }
  }
}

# --- 更新者の呼称(shared_appellations)・名前解決 --------------------------------
query TaskReadUpdaterAppellation($sharedAppellationsId: UUID!) {
  sharedAppellationBySharedAppellationsId(
    sharedAppellationsId: $sharedAppellationsId
  ) {
    sharedAppellationsId
    sharedDictionaryBySharedDictionaryNameId {
      ja
      en
    }
    sharedDictionaryBySharedDictionaryPronunciationId {
      ja
      en
    }
  }
}
