import 'package:mock_up/imports.dart';

class EditLocation extends ConsumerWidget {
  const EditLocation({super.key});
  @override
  Widget build(BuildContext context, WidgetRef ref) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
