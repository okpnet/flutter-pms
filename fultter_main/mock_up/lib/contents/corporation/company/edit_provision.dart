import '../../../imports.dart';

class EditProvision extends ConsumerWidget {
  const EditProvision({super.key});
  @override
  Widget build(BuildContext context, WidgetRef ref) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
